^ [[agentic engineering practice]] - <span class="link-not-tracked">link not tracked</span> to [[practice - using vibe coded slop for prototyping]] then re-integrate

---

While using <span class="link-not-tracked">link not tracked</span> , you might realize you have some unknowns that you could clear up with a quick prototype. So you could [[practice - using vibe coded slop for prototyping|use vibe coded slop for a prototype]].  This practice describes the _actual_ process for doing this.

Basically, you essentially want to trigger a [[subagent]] manually.

In you grilling session, you trigger the <span class="link-not-tracked">link not tracked</span>, which essentially does a compaction of the current context into a markdown file specifically for the new agent.  Then in the new agent you do the prototyping, then you invoke <span class="link-not-tracked">link not tracked</span> to hand back the findings to the original agent.

### Status 🔴

I have not tried this yet.  <span class="link-not-tracked">link not tracked</span> is great, and I _do_ find that occasionally I'd benefit from doing a quick prototype, but I haven't tried it.

### Sources

This process is described in more detail in <span class="link-not-tracked">link not tracked</span>