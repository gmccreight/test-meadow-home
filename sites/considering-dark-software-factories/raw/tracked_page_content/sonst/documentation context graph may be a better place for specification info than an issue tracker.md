^ [[documentation context graph]] may be a better place for specification info than an [[issue tracker]]

---

The [[documentation context graph -- document]] can be marked up in [[markdown extension -- front matter]] to make it's stage of processing clear.

The "issue tracker" can be more of an ephemeral "this is what happened" log than a storage of how things should work.  So more like reciepts.

The [[documentation context graph -- document]] that drove the bulk of the work can be included in the [[agent task -- change review app]]

