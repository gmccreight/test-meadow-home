https://x.com/Dimillian/status/2051590007308091521?s=20

^ [[compound engineering]] by [[company - OpenAI]] developer

---

Truly the biggest unlock is when the project is agent and worktree native. The Codex app team made sure this was the case. What does it mean?
...
[[agent native codebase]] [[worktree native codebase]]

It means that as soon as you click the new worktree button, the script will set up dependencies, etc., and the app will be ready to build and run on that worktree. The other thing is that this is a new stack, and it’s fast. Building takes 15 seconds, so you and the agent…
...
[[dim - speed -- fast]] project build in [[git worktree]]

The other big compounding pieces are documentation and project-scoped skills.
...
[[practice - context right in the repo]] and [[practice - project-scoped agent skill]]s

The agent feedback loop is also slower in those kinds of environment. If building and running tests is slower it means that the thread will run for longer for iterative changes.
...
[[fast-tests]]


